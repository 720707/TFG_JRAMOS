///Autor: Javier Ramos Marco
/// * Fichero con los textos de las distintas excepciones en caso de error
///

const textoUsuarioNoEncontrado =
    'No existe un usuario con esas credenciales, vuelve a intentarlo';

const textoPasswordIncorrecta =
    'La contraseña introducida es incorrecta, vuelve a intentarlo';

const textoEmailEnUso = 'El email introducido esta en uso, vuelve a intentarlo';

const textoEmailInvalido =
    'El email introducido no es valido, vuelve a intentarlo';

const textoExcepcionGenerica = 'Se ha producido un error, vuelve a intentarlo';

const textoUsuarioUsuarioAutenticadoNoHaIniciadoSesion =
    'Vuelve a iniciar sesion';

const textoNoSePuedeCrearAlumno =
    'No se ha podido cargar la lista de alumnos, vuelva a intentarlo';

const textoNoSePuedeObtenerAlumno =
    'No se ha podido obtener el alumno, vuelva a intentarlo';

const textoNoSePuedeCrearAsignatura =
    'No se ha podido crear la asignatura, vuelva a intentarlo';

const textoNoSePuedeObtenerAsignatura =
    'No se ha podido obtener la asignatura, vuelva a intentarlo';

const noSePuedeActualizarAsignatura =
    'No se ha podido actualizar la asignatura, vuelva a intentarlo';

const textoNoSePuedeEliminarAsignatura =
    'No se ha podido eliminar la asignatura, vuelva a intentarlo';

const textoNoSePuedeEditarListaAsistencia =
    'No se ha podido editar la lista de asistencias, vuelva a intentarlo';

const textoNoSePuedeCargarAsignatura =
    'No se ha podido cargar la asignatura, vuelva a intentarlo';

const noSePuedeGuardarListaAsistenciaDia =
    'No se ha podido guardar la lista de asistencia, vuelva a intentarlo';

const noSePuedeActualizarListaAsistenciaDia =
    'No se ha podido guardar la lista de asistencia, vuelva a intentarlo';

const textoNoSePuedeCrearClase =
    'No se han podido crear las clases, vuelva a intentarlo';

const noSePuedeCrearListaAsistenciaMes =
    'No se ha podido guardar la lista de asistencia para el mes, vuelva a intentarlo';

const noSePuedeActualizarListaAsistenciaMes =
    'No se ha podido actualizar la lista de asistencia para el mes, vuelva a intentarlo';

const textoNoSePuedeGenerarPDF =
    'No se ha podido generar el PDF, vuelve a intenterlo';

const textoNoSePuedeCargarCalendario =
    'No se ha podido cargar el calendario, vuelve a intenterlo';
